<UserControl x:Class="TMP.DWRES.Helper.Controls.AsyncBusyUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TMP.DWRES.Helper.Controls"
             Width="Auto"
             Height="Auto"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Border Background="White">
        <Grid Margin="0"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Background="White">
            <Viewbox Width="150"
                     Height="150"
                     Margin="10"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center">
                <local:CircularProgressBar />
            </Viewbox>
            <TextBlock x:Name="lblWait"
                       Margin="10 5"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Padding="0">
                <Bold>Пожалуйста,</Bold>
                <LineBreak />
                подождите ...
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
